from pathlib import Path


PATH = Path(__file__).resolve().parents[1] / "notes" / "Microeconomics_1_notes_by_Victor_Aguiar.lyx"
text = PATH.read_text()
start_marker = r"""\begin_layout Section
General Equilibrium in Pure Exchange with Inattention
 
\end_layout"""
end_marker = r"""\begin_layout Chapter
Positive Theory of Equilibrium
\end_layout"""
start = text.index(start_marker)
end = text.index(end_marker, start)

replacement = r'''\begin_layout Section
General Equilibrium with Inattention
\end_layout

\begin_layout Standard
An inattention model must distinguish the market prices that determine feasibility from the prices or signals used by a consumer's decision rule.  Let the economy have consumers \begin_inset Formula $i=1,\ldots,I$\end_inset
 and aggregate endowment \begin_inset Formula $\omega=\sum_i\omega_i$\end_inset
.
 For each consumer, let \begin_inset Formula $M_i$\end_inset
 denote an attention parameter (for example, a diagonal matrix with strictly positive entries) and let \begin_inset Formula $x^i(p,M_i)$\end_inset
 be the demand correspondence generated by the specified behavioral model.
 The notation is deliberately model-specific: one must state whether \begin_inset Formula $M_i$\end_inset
 distorts perceived prices, the budget constraint, utility, or information about the state.
\end_layout

\begin_layout Standard
For example, under a price-perception benchmark a consumer forms \begin_inset Formula $\widetilde p_i=M_i p$\end_inset
 and solves
\end_layout

\begin_layout Standard
\begin_inset Formula 
\[
x^i(p,M_i)\in\arg\max_{x\in X_i}\{u_i(x):\widetilde p_i\cdot x\leq\widetilde p_i\cdot\omega_i\}.
\]
\end_inset

\end_layout

\begin_layout Standard
This is a behavioral benchmark, not automatically a Walrasian budget.  If the actual budget uses \begin_inset Formula $p\cdot x\leq p\cdot\omega_i$\end_inset
 instead, that constraint must be written explicitly and the two price vectors must not be conflated.
\end_layout

\begin_layout Definition
(Inattention equilibrium.)  Given a fully specified demand correspondence \begin_inset Formula $x^i(p,M_i)$\end_inset
 and true prices \begin_inset Formula $p\gg0$\end_inset
,
 an inattention equilibrium is a price--allocation pair \begin_inset Formula $(p,x)$\end_inset
 such that each \begin_inset Formula $x_i\in x^i(p,M_i)$\end_inset
 satisfies the model's actual feasibility constraints and markets clear:
\begin_inset Formula 
\[
\sum_{i=1}^{I}x_i=\omega.
\]
\end_inset
\end_layout

\begin_layout Proposition
(Compatibility with Pareto efficiency.)  Suppose preferences are differentiable, strictly increasing, and concave, and consider an interior allocation \begin_inset Formula $x^*=(x_i^*)_i$\end_inset
.
 If it is Pareto efficient, then there is a common supporting price vector \begin_inset Formula $q\gg0$\end_inset
 and positive multipliers \begin_inset Formula $\mu_i$\end_inset
 such that
\begin_inset Formula 
\[
\nabla u_i(x_i^*)=\mu_i q\quad\text{for every }i.
\]
\end_inset
 If, in addition, the behavioral first-order condition is \begin_inset Formula $\nabla u_i(x_i^*)=\lambda_i M_i p$\end_inset
,
 then every \begin_inset Formula $M_i p$\end_inset
 must be collinear with the same \begin_inset Formula $q$\end_inset
.
\end_layout

\begin_layout Standard
The proposition gives a necessary compatibility condition, not an if-and-only-if result about attention matrices.  Identical matrices can be sufficient in a regular model when the common perceived price supports the allocation, but different matrices can also be compatible at a particular allocation when their distortions preserve the relevant relative prices.  Conversely, perceived-price optimality alone does not imply true-price optimality, so the First and Second Welfare Theorems cannot be invoked without checking the actual budget, feasibility, and convexity assumptions.
\end_layout

\begin_layout Standard
The associated aggregate excess demand is
\end_layout

\begin_layout Standard
\begin_inset Formula 
\[
z(p;M)=\sum_{i=1}^{I}x^i(p,M_i)-\omega.
\]
\end_inset
\end_layout

\begin_layout Standard
An equilibrium requires \begin_inset Formula $z(p;M)=0$\end_inset
 together with individual feasibility.  If the demand rule is not homogeneous of degree zero in the true price vector, prices cannot be reduced to a single relative-price normalization.  In that case the offer set is naturally indexed by both prices and wealth (or by the full attention state), and it need not be a one-dimensional curve.
\end_layout

\begin_layout Standard
The correct lesson is therefore diagnostic: specify the information and budget technology first, derive the resulting demand, and only then test market clearing, revealed-preference restrictions, and welfare conclusions.  No blanket equivalence between common attention parameters and Pareto efficiency follows from the notation alone.
\end_layout

'''
PATH.write_text(text[:start] + replacement + text[end:])
print(f"replaced inattention section in {PATH}")
