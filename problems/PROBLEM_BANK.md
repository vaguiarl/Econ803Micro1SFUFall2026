# Publication problem bank

This file is the editable source for the end-of-chapter problems in the Fall 2026 textbook. Each problem has a stable current identifier and one of three labels: Core, Proof, or Applied. The public book contains questions only; solutions are maintained in the private instructor repository. Legacy Fall 2024/Fall 2026-baseline numbers are recorded in `PROBLEM_ID_ALIASES.tsv`.

<!-- ECON803_PROBLEM_RESTRUCTURE:1 -->

# Utility Maximization and Consumer Demand

## 1.1 | Core | Budget scaling and Cobb-Douglas demand

Let $X=\mathbb{R}_{+}^{2}$ and $B(p,w)=\{x\in X:p\cdot x\leq w\}$, where $p\gg0$ and $w>0$.

(a) Prove that $B(\lambda p,\lambda w)=B(p,w)$ for every $\lambda>0$. Explain why every demand correspondence generated by preference maximization is homogeneous of degree zero in $(p,w)$.

(b) Suppose preferences are locally nonsatiated. Prove that every chosen bundle satisfies Walras' law, $p\cdot x=w$.

(c) For $u(x)=x_{1}^{\alpha}x_{2}^{1-\alpha}$ with $0<\alpha<1$, derive Marshallian demand and verify both properties directly.

## 1.2 | Core | CES curvature, demand, and expenditure shares

For $\rho<1$ and $\rho\ne0$, let $u_\rho(x)=(\tfrac12 x_1^\rho+\tfrac12 x_2^\rho)^{1/\rho}$, with the Cobb-Douglas limit used at $\rho=0$ and the boundary extension stated in the text. Write $\sigma=1/(1-\rho)$.

(a) Derive demand from the first-order conditions. Explain why the optimum is interior for $\rho<1$ and why maximizing the sum of powers is incorrect when $\rho<0$.

(b) At $p=(1,4)$ and $w=120$, calculate quantities and expenditure shares for $\sigma\in\{1/2,1,2\}$. Check budget exhaustion.

(c) Raise only the second price to 9. Recalculate the three demands and explain why a smaller quantity can have a larger expenditure share.

(d) Derive the own-price, cross-price, and wealth elasticities. Distinguish the elasticity of a quantity ratio from an individual demand elasticity.

## 1.3 | Applied | CES calibration and observational equivalence

At prices $p^0=(1,2,4)$, a household with wealth $w^0=120$ chooses $x^0=(30,25,10)$. Consider Chapter 1's CES representation with positive normalized weights.

(a) For a specified $\sigma>0$, derive the weights reproducing this observation.

(b) Calibrate the weights for $\sigma=1/2$ and $\sigma=2$. For each calibration predict demand at $p^1=(2,2,4)$ and wealth 120.

(c) Explain why fitting one observation does not identify substitution. Describe additional observations that could distinguish the models.

## 1.4 | Core | Substitutes, complements, and boundary choices

Consider separately $u^A(x)=2x_1+x_2$ and $u^B(x)=\min\{x_1/3,x_2\}$ on $\mathbb R_+^2$, at strictly positive prices and wealth.

(a) Derive the full demand correspondence for $u^A$, including the price ratio at which it is set-valued.

(b) Derive demand for $u^B$. At $p=(2,3)$ and $w=36$, compute both models' choices.

(c) Explain why interior differentiable tangency conditions do not characterize these optima.

(d) Obtain the ratio $x_1=3x_2$ as a CES limit. Explain why holding the usual taste weights fixed does not preserve unequal Leontief proportions.

## 1.5 | Applied | Subsistence and quasilinear corners

The first of two consumers has $u(x)=(x_1-1)^{1/3}(x_2-2)^{2/3}$ on $x\ge(1,2)$. The second has $u(x)=4\sqrt{x_1}+x_2$ on $\mathbb R_+^2$.

(a) Derive the first consumer's demand above subsistence expenditure. State what happens at and below that threshold.

(b) At $p=(2,1)$, compute the first consumer's demands and shares at wealth 10 and 22. Distinguish homotheticity in discretionary consumption from homotheticity in total consumption.

(c) Normalize the second consumer's numeraire price to one and write $r=p_1$. Derive demand for all $r,w>0$, including the zero-numeraire region.

(d) Identify where a zero wealth effect for the non-numeraire good is valid. Compare this restriction with the Stone--Geary model.

# Duality, Comparative Statics, and Welfare

## 2.1 | Core | Cobb-Douglas duality

For $u(x)=x_{1}^{\alpha}x_{2}^{1-\alpha}$ with $0<\alpha<1$, $p\gg0$, $w>0$, and $\overline{u}>0$, derive the Marshallian demand $x(p,w)$, indirect utility $v(p,w)$, Hicksian demand $h(p,\overline{u})$, and expenditure function $e(p,\overline{u})$.

(a) Verify Walras' law and the identity $e(p,v(p,w))=w$.

(b) Verify $h(p,v(p,w))=x(p,w)$.

(c) Verify Roy's identity and Shephard's lemma.

## 2.2 | Proof | Differential restrictions on demand

Let $x:\mathbb{R}_{++}^{L}\times\mathbb{R}_{++}\to\mathbb{R}_{+}^{L}$ be continuously differentiable and satisfy Walras' law.

(a) Differentiate Walras' law with respect to wealth and prices.

(b) Define the Slutsky matrix by $S(p,w)=D_{p}x(p,w)+D_{w}x(p,w)x(p,w)^{\top}$. Prove that $p^{\top}S(p,w)=0$.

(c) Explain which additional restrictions on $S$ follow from utility maximization and which of them do not follow from Walras' law alone.

## 2.3 | Proof | The Slutsky matrix for Cobb-Douglas demand

Using the Marshallian demand from Problem 2.1, compute $S(p,w)=D_{p}x+D_{w}x\,x^{\top}$.

(a) Show that $S$ is symmetric.

(b) Show directly that $z^{\top}Sz\leq0$ for every $z\in\mathbb{R}^{2}$.

(c) Find the null space of $S$ and interpret it using homogeneity.

## 2.4 | Proof | CES duality and compensated substitution

Use Chapter 1's CES utility, with positive normalized weights and $\sigma>0$.

(a) Derive $\mathcal P(p)$, expenditure, and Hicksian demand. Treat $\sigma=1$ separately and retain the constants implied by the utility normalization.

(b) Show that the Slutsky matrix has off-diagonal entries $S_{\ell k}=\sigma x_\ell x_k/w$ and diagonal entries $S_{\ell\ell}=-\sigma x_\ell(1-b_\ell)/p_\ell$. Prove symmetry and negative semidefiniteness using a weighted-variance argument. Identify its null space for strictly positive demand.

(c) Explain how compensated cross-price effects can be positive when uncompensated cross-price effects are negative.

(d) For equal weights, $\sigma=2$, initial prices $(1,3)$, wealth 90, and final prices $(2,3)$, calculate compensation to retain initial utility. Decompose the first quantity's change into a compensated change and the remaining wealth effect.

## 2.5 | Core | Translog shares and the regular price region

Consider a homothetic translog index with $\alpha_0=0$, $\alpha=(2/5,3/5)$, and $\Gamma=-\tfrac18\left(\begin{smallmatrix}1&-1\\-1&1\end{smallmatrix}\right)$.

(a) Derive shares and demands as functions of relative prices and wealth. Find the open interval of $p_1/p_2$ for which both shares are positive.

(b) Compute $H=\Gamma+\boldsymbol b\boldsymbol b^\top-\operatorname{diag}(\boldsymbol b)$. Prove negative semidefiniteness throughout that interval.

(c) At $p=(2,1)$ and $w=100$, calculate demand, Marshallian elasticities, and compensated elasticities. Verify adding up and homogeneity.

(d) Explain why setting a negative predicted share to zero while retaining the other formula does not generally give a valid extension.

## 2.6 | Applied | A translog approximation to CES demand

Fix a two-good CES model, a positive reference price vector $p^0$, and substitution elasticity $\sigma>0$. Write $b_1^0,b_2^0$ for the reference shares and use normalized prices $r_\ell=p_\ell/p_\ell^0$.

(a) Compute the gradient and Hessian of $\log\mathcal P_{\mathrm{CES}}$ with respect to log prices at $p^0$.

(b) Choose translog coefficients in $\log r$ so the log price index agrees with CES through second order at $p^0$.

(c) Verify the restrictions for homogeneity. Show that demand levels and local price responses agree at the reference observation.

(d) Explain why predictions can disagree for a large price change. State the regularity checks needed before making welfare comparisons.

# Revealed Preference and Recoverability

## 3.1 | Core | A two-observation WARP test

A consumer chooses $x^{1}=(2,2)$ at $(p^{1},w^{1})=((2,1),6)$ and $x^{2}=(1,5/2)$ at $(p^{2},w^{2})=((1,2),6)$.

(a) Verify that each observed bundle exhausts its budget.

(b) Determine whether each bundle was affordable at the other observation.

(c) Does the dataset satisfy WARP for single-valued demand? Identify the two direct revealed-preference comparisons that determine your answer.

## 3.2 | Core | Recovering a quasilinear utility

Normalize $p_{2}=1$ and suppose an interior demand system satisfies $x_{1}(p_{1},w)=\beta-\alpha p_{1}$, where $\alpha,\beta>0$, and Walras' law.

(a) Derive $x_{2}(p_{1},w)$ and state the domain on which both demands are nonnegative.

(b) Compute the normalized scalar Slutsky term for the non-numeraire demand.

(c) Recover a quasilinear utility representation $u(x_{1},x_{2})=g(x_{1})+x_{2}$ that generates the demand on the interior domain. Distinguish general ordinal uniqueness up to an increasing transformation from the narrower normalization freedom after the coefficient on $x_{2}$ is fixed at one.

## 3.3 | Core | Afriat inequalities by hand

Consider two observations: $p^{1}=(1,1)$, $x^{1}=(2,1)$ and $p^{2}=(2,1)$, $x^{2}=(1,1)$.

(a) Compute observed expenditures and all direct revealed-preference comparisons.

(b) Check GARP.

(c) Verify that $U^{1}=1$, $U^{2}=0$, and $\lambda^{1}=\lambda^{2}=1$ satisfy the Afriat inequalities.

(d) Write the corresponding piecewise-linear Afriat utility and verify that it assigns the required utility levels at the observations.

## 3.4 | Applied | Demand forecasting with Varian's support set

A finite dataset $\mathcal O=\{(p^{t},x^{t})\}_{t\in\mathbb T}$ satisfies GARP. Fix a counterfactual budget $(p^{0},w^{0})$ with $p^{0}\gg0$, and define $\mathcal S_{\mathcal O}(p^{0},w^{0})=\{x^{0}:p^{0}\cdot x^{0}=w^{0},\ \mathcal O\cup\{(p^{0},x^{0})\}\text{ satisfies GARP}\}$.

(a) State a revealed-preference condition under which the augmented data imply $x^{t}\succeq^R x^{0}$.

(b) Derive the associated linear restriction on $p^{0}\cdot x^{0}$.

(c) Explain why the restrictions in parts (a)--(b) are necessary but must be combined with every revealed-preference path into and out of the candidate observation to characterize $\mathcal S_{\mathcal O}(p^{0},w^{0})$. Explain why this set gives bounds rather than point identification.

## 3.5 | Core | Classical utility as singleton CMU

Let $u:X\to\mathbb R$ be continuous, let $\Omega_u=\{\{u\}\}$, and define
$r_{\Omega_u}(x,y)=\max_{U\in\Omega_u}\min_{v\in U}[v(x)-v(y)]$.

(a) Prove that $r_{\Omega_u}(x,y)=u(x)-u(y)$.

(b) Prove that $x_{r_{\Omega_u}}(p,w)=\arg\max_{x\in B(p,w)}u(x)$.

(c) Explain precisely which conclusions require continuity, strict monotonicity, or concavity of $u$, and which do not.

## 3.6 | Proof | WGARP without GARP

Consider prices $p^1=(4,1,5)$, $p^2=(5,4,1)$, $p^3=(1,5,4)$ and choices $x^1=(4,1,1)$, $x^2=(1,4,1)$, $x^3=(1,1,4)$.

(a) Compute the $3\times3$ matrix with $(t,s)$ entry $p^t\cdot x^s$.

(b) Show that the data satisfy WGARP and WARP but violate GARP. Identify the revealed three-cycle and explain why no pair forms a reversal.

(c) Suppose an asymmetric, strictly increasing preference function $r$ rationalizes two choices $x\in x_r(p,w)$ and $x'\in x_r(p',p'\cdot x)$. Prove the compensated law of demand $(p'-p)\cdot(x'-x)\leq0$. State where nonemptiness of demand is used.

# Choice Without Budget Geometry

## 4.1 | Core | Incomplete preferences and multiple utilities

Let $X=\{a,b,c\}$ and let $\notationfirst{binary-relation}{R}$ contain all reflexive pairs and the comparison $aRb$, but no other comparison between distinct alternatives.

(a) Show that $R$ is a preorder but is incomplete.

(b) Prove that no real-valued utility function can represent $R$ in the sense $xRy$ if and only if $u(x)\geq u(y)$.

(c) Construct two utility functions $u_{1},u_{2}$ such that $xRy$ if and only if $u_{k}(x)\geq u_{k}(y)$ for both $k=1,2$.

## 4.2 | Core | Choice cycles on finite menus

Let $X=\{a,b,c\}$ and suppose a single-valued choice function satisfies $c(\{a,b\})=a$, $c(\{b,c\})=b$, and $c(\{a,c\})=c$. Say that a preference relation $R$ rationalizes these data when the observed alternative is the unique $R$-greatest element of each observed menu.

(a) Write the direct revealed-preference comparisons generated by these choices.

(b) Show that no transitive preference relation can rationalize the data.

(c) State the smallest change to one observation that restores acyclicity, and exhibit a complete and transitive rationalization of the revised data.

## 4.3 | Proof | Maximization and WGARP

Let $R$ be complete and transitive on a finite set $X$. For each nonempty menu $A\subseteq X$, let $c(A)$ be the set of $R$-greatest elements of $A$.

(a) Prove that $c(A)$ is nonempty.

(b) Suppose $x\in c(A)$, $y\in c(B)$, and $x,y\in A\cap B$. Prove that $x\notationfirst{symmetric-part}{I_R}y$, where $xI_Ry$ means $xRy$ and $yRx$.

(c) Explain why this conclusion is WGARP, the appropriate weak axiom for a set-valued choice correspondence, and distinguish it from WARP for single-valued demand.

# Expected Utility and Risk

## 5.1 | Core | Affine expected-utility representations

Let $Z=\{z_{1},\ldots,z_{n}\}$ and suppose $U(\mu)=\sum_{j}\mu_{j}u(z_{j})$ represents a nontrivial preference on $\Delta(Z)$.

(a) Show directly that $U$ is affine in lotteries.

(b) Prove that $U'=aU+b$ with $a>0$ represents the same preference.

(c) Show that if two expected-utility representations agree on preferences, their Bernoulli indices differ by a positive affine transformation.

## 5.2 | Core | First-order stochastic dominance

Lotteries $\mu$ and $\nu$ on prizes $\{0,1,2\}$ have probabilities $\mu=(0.2,0.3,0.5)$ and $\nu=(0.3,0.4,0.3)$.

(a) Compare their cumulative distribution functions and determine which lottery first-order stochastically dominates the other.

(b) Prove the expected-utility ranking for every increasing Bernoulli utility by discrete summation by parts.

(c) Explain why strict dominance need not imply a strict preference for a weakly increasing utility.

## 5.3 | Proof | The Arrow-Pratt approximation

An agent with twice continuously differentiable utility $u$ and wealth $w$ faces a zero-mean risk $\varepsilon$ with variance $\sigma^{2}$. Define the monetary risk premium $\operatorname{RP}$ by the indifference equation

$u(w-\operatorname{RP})=\mathbb E[u(w+\varepsilon)].$

(a) Use second-order Taylor expansions to derive the approximation

$\operatorname{RP}\approx\tfrac12A(w)\sigma^{2},$

where $A(w)=-u''(w)/u'(w)$.

(b) State the regularity and small-risk qualifications needed for this approximation.

(c) Explain why the formula is invariant to positive affine transformations of $u$.

## 5.4 | Applied | CARA-normal portfolio demand

Terminal wealth is $W_T=wR_{f}+\theta(\widetilde R-R_{f})$, where $\widetilde R\sim N(m_R,\sigma^{2})$, $R_{f}$ is risk free, $\theta\in\mathbb R$ (borrowing and short selling are unrestricted), and utility is $u(W_T)=-\exp(-aW_T)$ with $a>0$.

(a) Show that maximizing expected utility is equivalent to maximizing a certainty equivalent.

(b) Derive optimal risky investment $\theta^{*}$.

(c) Give comparative statics with respect to $m_R-R_{f}$, $a$, and $\sigma^{2}$.

# Stochastic Choice and Random Utility

## 6.1 | Core | Logit odds and IIA

Choice probabilities satisfy $P(j\mid A)=\exp(V_{j}/\tau)/\sum_{k\in A}\exp(V_{k}/\tau)$, where $\tau>0$.

(a) Derive the odds ratio $P(j\mid A)/P(k\mid A)$.

(b) Prove the independence-of-irrelevant-alternatives property.

(c) Explain precisely which part of IIA is an empirical restriction and which part is an algebraic implication of the model.

## 6.2 | Applied | The red-bus blue-bus problem

Walking and a red bus initially have systematic utilities normalized so that each has probability one half under logit. A blue bus with the same systematic utility as the red bus is added.

(a) Compute the new logit probabilities.

(b) Compare the total bus share before and after entry.

(c) Explain why a model with highly correlated bus-specific shocks would predict a different substitution pattern.

## 6.3 | Core | A finite random-utility mixture

There are alternatives $a,b,c$. A population consists of three strict preference types: $a\succ b\succ c$ with mass $0.5$, $c\succ a\succ b$ with mass $0.2$, and $b\succ a\succ c$ with mass $0.3$.

(a) Compute choice probabilities from menus $\{a,b\}$ and $\{a,b,c\}$.

(b) Let the columns of $M$ correspond to all six strict orders on $\{a,b,c\}$, with zero masses allowed. Stack the observed probabilities as $\mathbf P$ and write $\mathbf P=M\lambda$.

(c) Explain why nonnegativity of $\lambda$ and $\mathbf 1_6^{\top}\lambda=1$ are the central finite random-utility feasibility restrictions.

# Limited Attention and Costly Information

## 7.1 | Core | Sparse-max quasilinear demand

Let $u(x_{1},x_{2})=\sqrt{x_{1}}+x_{2}$. The consumer uses perceived prices $\widehat p\gg0$ in the first-order condition but must satisfy the actual budget $p\cdot x=w$.

(a) Derive the interior sparse-max choice.

(b) State the condition under which the implied $x_{2}$ is nonnegative.

(c) Show that ordinary Marshallian demand is recovered when $\widehat p=p$.

## 7.2 | Core | CES demand under perceived prices

Let $u(x)=[\alpha x_{1}^{\rho}+(1-\alpha)x_{2}^{\rho}]^{1/\rho}$, where $0<\alpha<1$ and $\rho<1$, $\rho\ne0$.

(a) Use perceived prices $\widehat p$ to derive the chosen ratio $x_{1}/x_{2}$.

(b) Scale the bundle so that it exhausts the actual budget $p\cdot x=w$.

(c) Identify separately the perceived-price composition effect of changing $\widehat p$ and the feasibility effect of changing $p$. Explain why the first is not automatically a Hicksian substitution effect.

## 7.3 | Applied | A binary rational-inattention problem

There are two equally likely states and two actions. The decision maker receives payoff one from the correct action and zero from the incorrect action. A symmetric strategy is correct with probability $q\in[1/2,1]$ and pays Shannon-information cost $\kappa[\log2-H(q)]$, where $H(q)=-q\log q-(1-q)\log(1-q)$ and $\kappa>0$.

(a) Write expected net payoff as a function of $q$.

(b) Derive the interior optimum and show that $q^{*}=\exp(1/\kappa)/(1+\exp(1/\kappa))$.

(c) Describe the limits as $\kappa\downarrow0$ and $\kappa\uparrow\infty$.

# Aggregate Demand and Representative Consumers

## 8.1 | Core | Gorman aggregation

Suppose consumer $i$ has indirect utility $v_{i}(p,w_{i})=a_{i}(p)+b(p)w_{i}$, with the same positive function $b$ for every consumer.

(a) Apply Roy's identity to derive individual demand.

(b) Show that aggregate demand depends on the wealth distribution only through total wealth $W=\sum_{i}w_{i}$.

(c) Construct a representative indirect utility that generates aggregate demand.

## 8.2 | Core | Aggregation under common homothetic preferences

All consumers have the same homothetic preferences, strictly positive wealth $w_i>0$, and a single-valued demand. Normalize the unit-wealth demand as $x(p,1)$.

(a) Show that $x_{i}(p,w_{i})=w_{i}x(p,1)$.

(b) Derive aggregate demand and identify a representative consumer.

(c) Explain why common homotheticity is sufficient but not necessary for exact aggregation.

## 8.3 | Proof | Why uncompensated aggregate price derivatives need not be symmetric

Let $x^{A}(p)=\sum_{i}x_{i}(p,w_{i}(p))$, where individual wealth may vary with prices through endowments or profit shares.

(a) Differentiate $x^{A}$ with respect to prices.

(b) Separate compensated substitution terms from wealth-revaluation terms.

(c) Explain why symmetry and negative semidefiniteness of each individual Slutsky matrix do not generally imply the same properties for the uncompensated derivative $D_{p}x^{A}$.

# Theory of the Firm

## 9.1 | Core | Cobb-Douglas production and cost

A firm has production function $f(\boldsymbol z)=z_{1}^{\alpha}z_{2}^{\beta}$ with $\alpha,\beta>0$.

(a) Classify returns to scale as a function of $\alpha+\beta$.

(b) Derive conditional factor demands and the cost function for output $q>0$ and input prices $\boldsymbol w\gg0$.

(c) Verify that cost is homogeneous of degree one and concave in $\boldsymbol w$.

## 9.2 | Proof | Dual properties of the profit function

Let $Y\subseteq\mathbb R^{L}$ be nonempty, closed, and convex. Assume that profit $\pi_Y(p)=\sup_{y\in Y}p\cdot y$ is finite on a neighborhood of the price under study and that the supremum is attained.

(a) Prove that $\pi_Y$ is convex and homogeneous of degree one in prices.

(b) If supply is unique at $p$, use a subgradient argument to prove Hotelling's lemma and explain why the profit function is differentiable there.

(c) State what changes when supply is set-valued.

## 9.3 | Core | Constant returns and unbounded profit

Assume $Y$ is a cone and $0\in Y$.

(a) Prove that either $\pi_Y(p)=0$ or $\pi_Y(p)=+\infty$.

(b) Characterize the price vectors for which profit is zero.

(c) Explain why a competitive constant-returns firm can operate at any scale only when the zero-profit condition holds.

## 9.4 | Proof | Shephard's lemma and substitution

Suppose the minimum in $C(\boldsymbol w,q)=\min\{\boldsymbol w\cdot\boldsymbol z:f(\boldsymbol z)\geq q,\boldsymbol z\geq0\}$ exists, conditional factor demand $\mathcal Z(\boldsymbol w,q)$ is single-valued, and $C$ is twice differentiable in $\boldsymbol w$.

(a) Prove Shephard's lemma.

(b) Show that $D_{\boldsymbol w}\boldsymbol z=D^{2}_{\boldsymbol w\boldsymbol w}C$ is symmetric and negative semidefinite.

(c) Use homogeneity to show $D_{\boldsymbol w}\boldsymbol z(\boldsymbol w,q)\boldsymbol w=0$.

# Competitive Markets and Partial Equilibrium

## 10.1 | Core | Linear partial equilibrium

Demand and supply are $Q^{d}(p)=a-bp$ and $Q^{s}(p)=d(p-c)$ on their economically relevant ranges, with $a,b,d>0$, $c\geq0$, and $a>bc$.

(a) Solve for equilibrium price and quantity.

(b) Compute consumer and producer surplus using the choke price $a/b$ of inverse demand and the price intercept $c$ of inverse supply, so that the usual triangular-surplus geometry applies.

(c) Verify that the competitive quantity maximizes total surplus.

## 10.2 | Applied | A per-unit tax

Introduce a per-unit tax $t>0$ paid by sellers into the market of Problem 10.1, so buyers pay $p_{b}$ and sellers receive $p_{s}=p_{b}-t$. Assume $t$ is small enough that equilibrium quantity remains nonnegative.

(a) Solve for $p_{b}$, $p_{s}$, and quantity.

(b) Derive tax incidence. Express the local burden shares using the demand and supply slopes, and interpret them using point elasticities evaluated at the common no-tax equilibrium.

(c) Compute deadweight loss and its derivative with respect to $t$.

## 10.3 | Proof | Planner and competitive allocation

A representative consumer has quasilinear utility $u(q)+m$, with $u$ increasing and strictly concave, and production cost $C(q)$ is increasing and convex.

(a) State and solve the planner's problem using Kuhn-Tucker conditions.

(b) Show that any interior competitive equilibrium satisfies the same condition.

(c) State the assumptions required for the converse decentralization claim.

## 10.4 | Applied | The Economics of Superstars: Music Sales and Concerts

Taylor S. is a stylized superstar whose fans consume music services and concerts. This is the graduate version of the music-and-concerts case, with explicit preferences, technology, and market assumptions. All numbers are hypothetical; the model is not an empirical description of Taylor Swift's contracts. Quantities are divisible, and the mass of identical fans is one.

Part I. Fan demand. Write $q_M,q_C$ for music and concert services, the two coordinates of consumption. A fan has wealth $w>0$, prices $p_M,p_C>0$, and utility $u(q_M,q_C)=(\tfrac12 q_M^\rho+\tfrac12 q_C^\rho)^{1/\rho}$, where $0<\rho<1$. The letters $M,C$ are local commodity labels.

(a) Formulate the budget problem and derive demands using $\sigma=1/(1-\rho)$. For the remaining parts set $\rho=1/2$, $w=1$, and $p_M=1$. Explain why music demand changes when the concert price changes.

Part II. Production. For $j\in\{M,C\}$ the artist's technology is $y_j=A_j\sqrt{n_j}$, where $n_j\ge0$ is labor and $A_j>0$ productivity. Labor costs $\nu>0$ per hour, distinct from fan wealth $w$. The processes are independent. This stylized music-service cost does not describe the cost of creating an original recording as a function of its streams.

(b) Derive $C_j(\nu,y_j)$, average cost, and marginal cost. Explain decreasing returns. Set $A_M=A_C=\nu=1$ in the numerical parts.

Part III. A share of volume and receipts. An exogenous $\tau\in(0,1]$ is the fraction of music consumption attributable to this artist. Her output is $y_M=\tau q_M$ and her music receipts are $\tau p_Mq_M$. Concert output is $y_C=q_C$. She takes $p_M,\tau$ as given and chooses $p_C>0$. There is no shared labor constraint or fixed cost.

(c) Write total revenue and profit after substituting demand and costs. Distinguish a revenue derivative with respect to ticket price from marginal revenue with respect to output. Account for changes in both revenue streams and production costs.

(d) Derive the first-order condition for the optimal concert price. Examine profit as $p_C\downarrow0$ and $p_C\to\infty$. Establish global optimality and uniqueness, analytically or with a justified numerical method accounting for every stationary point and boundary.

(e) Compare $\tau=1$ and $\tau=1/2$. In a table report optimal concert prices, both fan quantities, both delivered outputs, revenues, costs, and profits. State numerical accuracy. Does the concert price rise or fall when the music share falls?

(f) Use the first-order condition or implicit differentiation to analyze the price response for $\tau\in[1/2,1]$. Interpret the roles of substitution and production costs. Do not assume the same sign for every technology or parameter value.

Part IV. A royalty share. Now $\tau$ changes only the fraction of music revenue paid to the artist: music output is $y_M=q_M$, while receipts remain $\tau p_Mq_M$. All other assumptions are unchanged.

(g) Write the revised profit function and repeat the comparison between $\tau=1$ and $\tau=1/2$. Identify the changed cost channel and determine whether the earlier price response survives.

(h) Explain what evidence distinguishes a fall in music volume from a fall in the royalty fraction. Discuss the roles of CES substitution, decreasing returns, and control of concert prices. Contrast the artist's decision with price-taking supply in the main chapter.

# General Equilibrium Theory

## 11.1 | Core | Cobb-Douglas exchange equilibrium

There are two consumers with common utility $u_{i}(x_{i1},x_{i2})=x_{i1}^{\alpha}x_{i2}^{1-\alpha}$, $0<\alpha<1$, and endowments $\omega_{1}=(1,2)$, $\omega_{2}=(2,1)$.

(a) Derive individual Walrasian demands.

(b) Normalize $p_{2}=1$ and solve for the equilibrium relative price.

(c) Compute equilibrium allocations and verify both market-clearing equations.

## 11.2 | Proof | First Fundamental Welfare Theorem

Consider a private-ownership economy with locally nonsatiated preferences and profit-maximizing firms.

(a) State the definition of Pareto efficiency used in the theorem.

(b) Prove that a competitive equilibrium allocation is Pareto efficient.

(c) Identify exactly where local nonsatiation, budget feasibility, profit maximization, and market clearing enter the proof.

## 11.3 | Proof | Supporting a Pareto optimum

Suppose consumption sets and production sets are closed and convex, preferences are continuous, convex, and locally nonsatiated, and a feasible allocation is Pareto efficient. Assume the separation hypotheses stated in the chapter.

(a) Formulate the aggregate set-separation argument and show how it supports the allocation as a price quasi-equilibrium with transfers.

(b) Explain why the initial endowment distribution generally must be changed by lump-sum transfers.

(c) State the positive-wealth or boundary condition needed to upgrade the quasi-equilibrium to a price equilibrium with transfers, and distinguish that object from a competitive equilibrium for the original ownership economy.

## 11.4 | Applied | One consumer and one producer

There are two goods. Good 1 can be consumed or used as input $z$ to produce good 2 according to $y_{2}=\sqrt z$. The consumer owns the firm and has an endowment $\bar\omega_{1}>0$ of good 1 and no good 2. Utility is $\log x_{1}+\log x_{2}$.

(a) Solve the planner's problem.

(b) Find supporting relative prices from marginal conditions.

(c) Verify that the consumer and firm choices clear both markets.

# Positive Theory of Equilibrium

## 12.1 | Core | Differential identities for excess demand

Let aggregate excess demand $z:\mathbb R_{++}^{L}\to\mathbb R^{L}$ be continuously differentiable, homogeneous of degree zero, and satisfy Walras' law.

(a) Differentiate homogeneity to show $D_{p}z(p)p=0$.

(b) Differentiate Walras' law to show $[D_{p}z(p)]^{\top}p=-z(p)$.

(c) Specialize the second identity to an equilibrium and interpret the left null vector.

## 12.2 | Proof | Regularity and local uniqueness

Normalize prices by $p_{L}=1$ and let $\widehat z$ be the first $L-1$ excess-demand equations.

(a) Define a regular equilibrium.

(b) Use the inverse function theorem to prove that a regular equilibrium is locally isolated.

(c) Explain why local isolation implies finitely many equilibria only after compactness and boundary conditions are added.

## 12.3 | Proof | Gross substitutes and uniqueness

Assume aggregate excess demand satisfies strict gross substitutes on the relevant price domain: increasing one price strictly raises excess demand for every other good whenever the comparison is nondegenerate. Suppose two strictly positive normalized equilibrium price vectors exist.

(a) Compare the largest relative price change across goods.

(b) Use gross substitutes and Walras' law to derive a contradiction unless the two price vectors are proportional.

(c) Explain why weak gross substitutes alone need not imply uniqueness and where strictness (or an irreducibility substitute) enters the proof.

## 12.4 | Core | Competitive allocations and the strong core

Use the strong-blocking convention in a finite pure-exchange economy with locally nonsatiated preferences: a coalition blocks when all its members can be made weakly better off using their own aggregate endowment and at least one member is made strictly better off. Define the associated strong core.

(a) Prove that every Walrasian allocation belongs to the strong core.

(b) Give an economic reason why the converse can fail with finitely many consumers.

(c) State the content, but not a proof, of the core-convergence theorem for replica economies.

# Testable Restrictions on the Equilibrium Manifold

## 13.1 | Core | Equilibrium Afriat feasibility

At each observation $t$, prices $p^{t}\gg0$, individual incomes $(w_i^t)_i$, and aggregate endowment $\omega^{t}$ are observed, while individual allocations are latent.

(a) Introduce latent consumptions $x_{i}^{t}$ and write individual budget equalities $p^t\cdot x_i^t=w_i^t$ and aggregate market clearing $\sum_i x_i^t=\omega^t$.

(b) Write Afriat inequalities for every consumer across observations.

(c) Explain why joint feasibility is a finite semialgebraic revealed-equilibrium test, why the latent multipliers and bundles make the full system bilinear, and how quantifier elimination projects the restrictions onto the observables.

## 13.2 | Proof | What equilibrium data identify

Suppose only equilibrium prices and aggregate endowments are observed.

(a) List the preference, allocation, and ownership objects that remain unobserved.

(b) Explain why market clearing alone cannot identify individual demand.

(c) Describe how additional observations, known individual endowments, or parametric restrictions can turn an underidentified model into a testable one.

## 13.3 | Applied | Counterfactual discipline

A dataset passes a revealed-equilibrium feasibility test under a maintained class of preferences.

(a) Explain why passing the test does not point-identify a counterfactual equilibrium.

(b) Define an identified set for a counterfactual price or allocation.

(c) State two economically meaningful restrictions that could tighten this set, and explain the cost of each restriction.

# Sequential Trade

## 14.1 | Core | Spanning contingent consumption

There are $S$ states tomorrow and $K$ traded assets with payoff matrix $R\in\mathbb R^{S\times K}$ and date-zero asset-price vector $q\in\mathbb R^{K}$. Assume no arbitrage.

(a) State the rank condition under which every contingent net trade can be implemented by an asset portfolio.

(b) If $S=2$ and the assets have payoff vectors $(1,1)$ and $(1,0)$, construct the portfolio that delivers any target payoff $(c_{1},c_{2})$.

(c) Let $\psi\in\mathbb R_{++}^{S}$ be state prices satisfying $q=R^{\top}\psi$. Explain how the date-zero portfolio budget maps into an Arrow-Debreu present-value budget.

## 14.2 | Core | Incomplete markets

Let $S=3$ and suppose the only assets are a bond with payoff $(1,1,1)$ and an asset with payoff $(1,0,0)$.

(a) Characterize the attainable payoff subspace.

(b) Determine whether the claim $(0,1,-1)$ is attainable.

(c) Explain why competitive equilibrium with these assets need not be Pareto efficient relative to the full contingent-commodity economy.

## 14.3 | Proof | Arrow-Debreu and sequential equivalence

There are $S$ states, $K$ assets with payoff matrix $R\in\mathbb R^{S\times K}$ of rank $S$, date-zero asset prices $q$, and state-$s$ spot-price vectors $p_s$. Asset payoffs are denominated in the state numeraire. Consumer $i$ chooses a portfolio $z_i$ subject to $q\cdot z_i\leq0$, then chooses $x_{is}$ subject to $p_s\cdot x_{is}\leq p_s\cdot\omega_{is}+p_{s1}(Rz_i)_s$. Assume no arbitrage and strictly monotone continuous preferences.

(a) Map an Arrow-Debreu budget into date-zero asset purchases and state-by-state spot trades.

(b) Prove the reverse mapping from a sequential budget to a present-value budget.

(c) Identify which step fails under incomplete markets or binding portfolio constraints.

# Matching

## 15.1 | Core | Deferred acceptance by hand

Three men have rankings $m_{1}:w_{1}\succ w_{2}\succ w_{3}$, $m_{2}:w_{1}\succ w_{2}\succ w_{3}$, and $m_{3}:w_{2}\succ w_{3}\succ w_{1}$. The women rank $w_{1}:m_{2}\succ m_{1}\succ m_{3}$, $w_{2}:m_{1}\succ m_{3}\succ m_{2}$, and $w_{3}:m_{3}\succ m_{2}\succ m_{1}$.

(a) Run men-proposing deferred acceptance round by round.

(b) Verify stability of the resulting matching by checking potential blocking pairs.

(c) Explain why termination is guaranteed in every finite market.

## 15.2 | Proof | Proposer optimality

Consider a finite balanced one-to-one matching market in which every participant has a complete strict ranking of the other side and all partners are acceptable.

(a) Prove that if a receiver rejects a proposer during deferred acceptance, that rejected proposer-receiver pair cannot be matched to each other in any stable matching.

(b) Use this rejection lemma to prove proposer optimality among stable matchings.

(c) Explain why the receiver side obtains its least-preferred stable matching.

## 15.3 | Core | Top trading cycles

Agents $1,2,3$ initially own houses $h_{1},h_{2},h_{3}$. Their rankings are $1:h_{2}\succ h_{1}\succ h_{3}$, $2:h_{1}\succ h_{2}\succ h_{3}$, and $3:h_{1}\succ h_{3}\succ h_{2}$.

(a) Run top trading cycles.

(b) Show that the outcome is individually rational and Pareto efficient.

(c) Verify directly that no coalition blocks the outcome.

# Computable General Equilibrium

## 16.1 | Core | Calibrating a CES technology

A constant-returns technology is $Q=A[\alpha K^{-\rho}+(1-\alpha)n^{-\rho}]^{-1/\rho}$, with $\rho>-1$ and $\rho\ne0$. A strictly positive interior cost-minimizing benchmark observation gives $(Q,K,n,r,W)$ and an elasticity of substitution $\sigma>0$, $\sigma\ne1$. (The Cobb-Douglas limit $\sigma=1$ is outside the displayed parameterization.)

(a) Recover $\rho$ from $\sigma$.

(b) Use the cost-minimizing first-order condition to recover $\alpha$.

(c) Recover $A$ and verify that the benchmark is exactly replicated.

## 16.2 | Core | Social-accounting consistency

A two-sector social accounting matrix records sectoral sales, intermediate purchases, labor and capital payments, household income, taxes, savings, and imports.

(a) State the row-balance and column-balance conditions.

(b) Explain why every market-clearing equation has a corresponding accounting identity but not every accounting identity is a behavioral equation.

(c) Design a numerical residual check that would detect an incorrectly calibrated benchmark.

## 16.3 | Applied | A tariff counterfactual

A small open economy takes the world price $P^{W}$ and exchange rate $\mathrm{ER}$ as given. A tariff $\tau$ implies domestic price $P=\mathrm{ER}\,P^{W}(1+\tau)$. Let differentiable domestic demand and supply be $D(P)$ and $S(P)$, imports be $M(P)=D(P)-S(P)>0$, and tariff revenue be returned lump-sum, $\mathrm{TR}(\tau)=\tau\,\mathrm{ER}\,P^{W}M(P(\tau))$. Interpret $D$ as quasi-linear (or otherwise free of income effects) and $S$ as competitive supply generated by marginal cost, so consumer and producer surplus are valid partial-equilibrium welfare measures.

(a) Derive the first-order change in the domestic price from a small tariff change.

(b) Derive the symbolic first-order decomposition $d\mathcal W=d\,\mathrm{CS}+d\,\mathrm{PS}+d\,\mathrm{TR}$ and simplify it at free trade. Do not assume numerical functional forms.

(c) Explain which general-equilibrium channels are omitted by that approximation and must be restored in a CGE counterfactual.

# Order Theory

## A.1 | Core | Classifying relations

Assume $|X|\geq2$. For each relation below, determine whether it is reflexive, irreflexive, symmetric, asymmetric, antisymmetric, transitive, and complete: equality on $X$; strict set inclusion on $2^{X}$; weak set inclusion on $2^{X}$; and the componentwise order on $\mathbb R^{2}$.

Give a counterexample for every failed property.

## A.2 | Proof | Quotienting a preorder

Let $R$ be a preorder on $X$ and define $xI_Ry$ if and only if $xRy$ and $yRx$.

(a) Prove that $I_R$ is an equivalence relation.

(b) Define $[x]\,\bar R\,[y]$ if and only if $xRy$. Prove that this definition is independent of representatives.

(c) Prove that $\bar R$ is a partial order on $X/I_R$.

## A.3 | Proof | Maximal, greatest, and finite existence

Let $R$ be a preorder on a nonempty finite set $S$.

(a) Prove that $S$ has an $R$-maximal element.

(b) Give an example with several maximal elements and no greatest element.

(c) Prove that if $R$ is complete, maximal and greatest elements coincide.

## A.4 | Proof | Order extensions and multiple representation

Let $R$ be a preorder on a finite set $X$.

(a) Pass to the quotient partial order on $X/I_{R}$ and extend it to a linear order.

(b) Pull the extension back to a complete preorder on $X$.

(c) Show that $R$ is the intersection of its complete preorder extensions and deduce a finite multiple-utility representation.
